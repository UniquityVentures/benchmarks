[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "benchmark_app"
version = "0.0.1"
description = "Benchmark App for Frappe"
authors = [{name = "Benchmark Team"}]
dependencies = []
