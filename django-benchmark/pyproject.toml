[project]
name = "django-benchmark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.7",
    "psycopg[binary]",
    "gunicorn",
    "uvicorn",
    "gevent>=26.5.0",
]
